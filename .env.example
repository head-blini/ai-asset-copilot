# Phase 0: required environment variables = none.
# No API clients, credentials, database connection, or dotenv loader exists yet.
# Add named settings only when their owning Phase defines and implements them.
# Never commit real credentials or personal account data.
